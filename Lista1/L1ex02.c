#include <stdio.h> //declaração de inclusão da biblioteca principal (ENTRADA E SAIDA de dados)
int main() //inicia o programa
{
	int x; //declara uma variável do tipo inteiro
	scanf("%d",&x); //entrada de um valor inteiro do dispositivo de entrada de dados
	printf("%d",x); //imprime na tela o valor digitado
	return(0); //retorna "0" significa que concluiu com exito o programa
}
